streamlit==1.55.0
# Community Cloud: pin protobuf to avoid spawn error from version conflict
protobuf>=3.20,<6
pandas==2.3.3
numpy==1.26.4
matplotlib==3.8.1
seaborn==0.12.1
scipy==1.15.3
plotly==6.6.0
# Kaleido 1.x: PNG export (로컬에서 Chrome 필요). 실패 시 Playwright 폴백 사용(Cloud 등)
kaleido==1.2.0
playwright>=1.40.0
openpyxl==3.1.2
Pillow==12.1.0
opencv-python==4.11.0.86
# Optional: OCR packages for legend text extraction
# pytesseract>=0.3.10  # Requires Tesseract OCR to be installed separately
# easyocr>=1.7.0  # Alternative OCR library
