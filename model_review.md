# 모델 파라미터 및 출력 데이터 검토 보고서

본 문서는 `General Analysis Mode`에서 사용되는 6가지 키네틱 모델의 수식과 이에 대응하는 필수 파라미터가 결과 테이블에 올바르게 반영되었는지 검토한 결과를 정리합니다.

## 1. 검토 개요

- **목적**: 피팅 완료 후 출력되는 데이터가 각 모델의 수식에 필요한 모든 상수를 포함하고 있는지 확인하고, 불필요한 컬럼은 배제하여 가독성을 높임.
- **변경 사항**: `mode_general_analysis/plot.py`의 `create_comparison_table` 함수 수정.
  - 모델별 필수 파라미터 정의.
  - 해당 모델에 필요 없는 파라미터는 `'-'`로 표시.
  - 값이 없는 경우 `None` 처리.

---

## 2. 모델별 상세 검토

각 모델의 지배 방정식(Governing Equation)과 피팅을 통해 도출되는 필수 파라미터(Required Parameters)는 다음과 같습니다.

### 📌 Model A: 기질 고갈 (Substrate Depletion)

가장 기본적인 1차 반응 모델로, 효소 농도에 비례하는 속도로 기질이 고갈됩니다.

- **수식**:
  $$ \alpha(t) = 1 - e^{-k*{obs} t}, \quad k*{obs} = \frac{k\_{cat}}{K_M}[E] $$
- **필수 파라미터**:
  - `kcat_KM`: 촉매 효율 ($M^{-1}s^{-1}$)
- **결과 테이블 처리**: `kcat_KM`만 값 표시, 나머지는 `-`.

### 📌 Model B: 효소 비활성화 (Enzyme Deactivation)

반응 중 효소가 서서히 활성을 잃어가는 현상을 반영합니다.

- **수식**:
  $$ \alpha(t) = 1 - \exp\left[-\frac{k\_{cat}/K_M [E]\_0}{k_d} (1 - e^{-k_d t})\right] $$
- **필수 파라미터**:
  - `kcat_KM`: 촉매 효율 ($M^{-1}s^{-1}$)
  - `kd`: 효소 비활성화 속도 상수 ($s^{-1}$)
- **결과 테이블 처리**: `kcat_KM`, `kd` 표시.

### 📌 Model C: 물질전달 제한 (Mass-Transfer Limitation)

효소가 기질 표면으로 확산되는 속도가 반응 속도를 제한하는 경우입니다.

- **수식**:
  $$ [E]_s \approx \frac{[E]\_b}{1 + Da}, \quad Da = \frac{k_{cat} \Gamma_0}{K_M k_m} $$
- **필수 파라미터**:
  - `kcat_KM`: 촉매 효율 ($M^{-1}s^{-1}$)
  - `km`: 물질전달 계수 ($m/s$)
  - `Gamma0`: 초기 표면 기질 밀도 ($pmol/cm^2$)
- **결과 테이블 처리**: `kcat_KM`, `km`, `Gamma0` 표시.

### 📌 Model D: 농도 의존 Fmax (Concentration-Dependent Fmax)

효소 농도가 높을수록 더 깊은 겔 층까지 침투하여 최대 반응(Fmax)이 증가하는 모델입니다.

- **수식**:
  $$ \alpha(t) = \alpha*{max}([E]) \cdot (1 - e^{-k*{obs} t}) $$
    $$ \alpha*{max}([E]) = \alpha*{\infty} \cdot (1 - e^{-k\_{access} [E]}) $$
- **필수 파라미터**:
  - `kcat_KM`: 촉매 효율 ($M^{-1}s^{-1}$)
  - `alpha_inf`: 이론적 최대 절단 비율 (단위 없음)
  - `k_access`: 접근성 계수 ($M^{-1}$)
- **결과 테이블 처리**: `kcat_KM`, `alpha_inf`, `k_access` 표시.

### 📌 Model E: 생성물 억제 (Product Inhibition)

절단된 펩타이드 조각(생성물)이 효소의 활성 부위를 경쟁적으로 억제하는 모델입니다.

- **수식**:
  $$ \frac{d\alpha}{dt} = \frac{k*{obs}(1-\alpha)}{1 + K*{i,eff}\alpha} $$
- **필수 파라미터**:
  - `kcat_KM`: 촉매 효율 ($M^{-1}s^{-1}$)
  - `Ki_eff`: 유효 억제 상수 (단위 없음)
- **결과 테이블 처리**: `kcat_KM`, `Ki_eff` 표시.

### 📌 Model F: 효소 표면 흡착/격리 (Enzyme Surface Sequestration)

효소가 표면에 비가역적으로 흡착되어 반응에 참여하지 못하게 되는 모델입니다.

- **수식**:
  $$ \alpha(t) \approx \frac{(k*{cat}/K_M)[E]}{k*{ads}(1+K*{ads}[E])} (1 - e^{-k*{ads} t}) $$
- **필수 파라미터**:
  - `kcat_KM`: 촉매 효율 ($M^{-1}s^{-1}$)
  - `k_ads`: 흡착 속도 상수 ($s^{-1}$)
  - `K_ads`: 흡착 평형 상수 ($M^{-1}$)
- **결과 테이블 처리**: `kcat_KM`, `k_ads`, `K_ads` 표시.

---

## 3. 데이터 출력 로직 변경 사항

모든 파라미터를 포함하는 마스터 리스트를 정의하고, 각 모델 결과 출력 시 해당 모델과 관련 없는 파라미터는 `'-'`로 마스킹 처리하였습니다.

| 파라미터      | Model A | Model B | Model C | Model D | Model E | Model F |
| :------------ | :-----: | :-----: | :-----: | :-----: | :-----: | :-----: |
| **kcat_KM**   |   ✅    |   ✅    |   ✅    |   ✅    |   ✅    |   ✅    |
| **kd**        |    -    |   ✅    |    -    |    -    |    -    |    -    |
| **km**        |    -    |    -    |   ✅    |    -    |    -    |    -    |
| **Gamma0**    |    -    |    -    |   ✅    |    -    |    -    |    -    |
| **alpha_inf** |    -    |    -    |    -    |   ✅    |    -    |    -    |
| **k_access**  |    -    |    -    |    -    |   ✅    |    -    |    -    |
| **Ki_eff**    |    -    |    -    |    -    |    -    |   ✅    |    -    |
| **k_ads**     |    -    |    -    |    -    |    -    |    -    |   ✅    |
| **K_ads**     |    -    |    -    |    -    |    -    |    -    |   ✅    |

위 로직은 `mode_general_analysis/plot.py`에 적용되었습니다.
