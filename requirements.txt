streamlit==1.55.0
# Community Cloud 권장: protobuf 버전 제한 (미지정 시 충돌로 spawn error 유발 가능)
protobuf>=3.20,<6
pandas==2.3.3
numpy==1.26.4
matplotlib==3.8.1
seaborn==0.12.1
scipy==1.15.3
plotly==6.6.0
# Kaleido 1.x fixes PNG export on Windows (0.2.x can freeze/fail). Kaleido 1.x requires Chrome/Chromium; run: plotly_get_chrome
kaleido==1.2.0
openpyxl==3.1.2
Pillow==12.1.0
opencv-python==4.11.0.86
# Optional: OCR packages for legend text extraction
# pytesseract>=0.3.10  # Requires Tesseract OCR to be installed separately
# easyocr>=1.7.0  # Alternative OCR library
